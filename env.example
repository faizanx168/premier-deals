# Database
DATABASE_URL="postgresql://username:password@localhost:5432/premier_deals"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Email Service (SMTP)
# If using the built-in mailer, set these values:
SMTP_ENDPOINT="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USERNAME="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
FROM="Premier Deals <no-reply@premierdeals.pk>"

# (Optional) If you prefer alternative variable names, ensure your code reads them.
# SMTP_HOST, SMTP_USER, and SMTP_PASS are kept for reference/compatibility but are not used by src/lib/email.ts
SMTP_HOST="smtp.gmail.com"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# For connection pooling (recommended for production)
DIRECT_URL="postgresql://username:password@localhost:5432/premier_deals" 